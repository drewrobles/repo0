import { LocalStorageTextField } from '../LocalStorageTextField'

export default {
  title: 'Forms/LocalStorageTextField',
  component: LocalStorageTextField,
  tags: ['autodocs'],
};

export const Primary = {
  args: {
    primary: true,
    label: 'latitude',
  },
};
